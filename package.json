{
  "name": "effects-as-data-redux",
  "version": "1.0.2",
  "description": "Easily connect redux and effects-as-data.",
  "main": "lib/index.js",
  "scripts": {
    "test": "jest --coverage",
    "transpile": "babel src --out-dir lib --source-maps",
    "deploy": "npm run transpile && npm test && npm publish; rm -rf lib"
  },
  "keywords": [],
  "author": "Frankie O'Rourke",
  "license": "MIT",
  "dependencies": {
    "object-path": "^0.11.4"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-jest": "^20.0.0",
    "babel-preset-es2015": "^6.24.1",
    "jest-cli": "^20.0.0",
    "sinon": "^2.4.1"
  },
  "jest": {
    "testPathIgnorePatterns": ["<rootDir>/src/test.js", "<rootDir>/lib/test.js"]
  }
}
